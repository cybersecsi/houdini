{
  "name": "houdini",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:cybersecsi/HOUDINI.git",
  "license": "GPL-3.0",
  "scripts": {
    "start": "node copy_config.js && cd webapp && yarn run start",
    "update": "node copy_config.js",
    "build": "node copy_config.js && cd webapp && yarn run build && node sitemap-builder",
    "bootstrap": "node bootstrap.js"
  },
  "dependencies": {
    "fs-extra": "^10.0.0",
    "inquirer": "^8.2.0"
  }
}
